### DNN dataset config ----------------------------------------------------
# These are config parameters for the dnn dataset generator

dnn_dataset_config <- list(
  seq_max_len = 10,     
  skip_val = 1,         
  prop_train = 0.9,
  prop_train_cds = 0.5
)
